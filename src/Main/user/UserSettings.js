import React from 'react';

const UserSettings = () => {
    return (
        <div>
            <h2>Settings</h2>
            {/* Add settings options here */}
        </div>
    );
};

export default UserSettings;
